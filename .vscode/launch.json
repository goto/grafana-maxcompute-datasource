{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Standalone debug mode",
      "type": "go",
      "request": "launch",
      "mode": "debug",
      "program": "${workspaceFolder}/pkg",
      "envFile": "${workspaceFolder}/.env",
      "cwd": "${workspaceFolder}",
      "args": [
        "-standalone"
      ]
    }
  ]
}